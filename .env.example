# Copy this file to .env and fill in real values. Never commit .env to git.

SECRET_KEY=change-this-to-a-long-random-string
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost,yourdomain.com
CSRF_TRUSTED_ORIGINS=

# Leave empty for local SQLite. On Render/Railway, set this to the Postgres URL they give you.
DATABASE_URL=

# Optional: for product/review photo uploads to survive on Vercel (serverless
# filesystem is read-only/ephemeral). Free tier at https://cloudinary.com —
# leave blank for local development, where uploads save to the media/ folder.
CLOUDINARY_URL=

# Business WhatsApp number customers are redirected to for order confirmation
# (format: countrycode+number, no + or spaces, e.g. 919201461413)
WHATSAPP_BUSINESS_NUMBER=919201461413

# Used in sitemap.xml / robots.txt links — set to your real domain once you have one
SITE_DOMAIN=bestonegifted.onrender.com
